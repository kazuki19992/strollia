# AGENTS.md

このリポジトリで作業するエージェントは、以下のルールに従うこと。

## 1. コミット方針

作業は、キリの良いタイミングでコミットできる粒度に分ける。

コミット粒度は大きすぎず小さすぎないことを意識する。

目安は以下とする。

- 1つのコミットは、1つの目的・1つの意味のある変更にまとめる
- 仕様書追加、依存関係追加、DB基盤、GPS記録、マップ表示、エクスポートなどは別コミット候補とする
- 動かない途中状態や壊れた状態をコミットしない
- 無関係な変更を同じコミットに混ぜない
- コミット前に、可能な範囲で型チェック・テストを実行する

コミットメッセージは Semantic Commit Message の形式に従う。

形式は以下とする。

```text
type(scope): 内容
```

ルールは以下とする。

- `type` は英語で記載する
- `type` は `feat`, `fix`, `docs`, `test`, `refactor`, `chore`, `build`, `ci` などから適切に選ぶ
- `scope` は必要な場合のみ英語で記載する
- `type` と `scope` 以外の説明部分は日本語で記載する
- 変更内容があとから追いやすい簡潔な文にする

例:

```text
docs: MVP仕様を追加
chore: Expo TypeScriptプロジェクトを初期化
feat(db): GPSログのSQLite保存を追加
feat(map): 日別ルートのマップ表示を追加
test(export): GPX生成のテストを追加
```

## 2. テスト方針

コードを追加・変更する場合は、必ず対応するテストコードを用意する。

テストが必要な対象は以下を含む。

- ビジネスロジック
- データ変換処理
- GPX / KML 生成処理
- 日付・時刻処理
- 距離計算
- DBリポジトリ
- UIコンポーネントの重要な振る舞い

外部依存が強い処理は、直接テストしにくい部分を分離する。

例:

- GPS取得APIそのものではなく、取得結果を保存用データに変換する関数をテストする
- ファイル共有APIそのものではなく、GPX文字列生成をテストする
- SQLiteアクセスはリポジトリ単位でテストし、必要に応じてテストDBまたはモックを使う

テストを書けない場合は、その理由を明記し、代替の検証方法を提示する。

## 3. 設計方針

コードベースは、モダンで保守しやすい設計を維持する。

基本方針は以下とする。

- TypeScriptの型を活用する
- UI、サービス、リポジトリ、ユーティリティを適切に分離する
- UIコンポーネントにDB操作や端末API呼び出しを直接詰め込まない
- 副作用を持つ処理はサービス層またはリポジトリ層に寄せる
- 純粋関数にできる処理は純粋関数として切り出す
- 将来のバックグラウンド記録、インポート、写真ジオタグ表示を追加しやすい構造にする
- 早すぎる抽象化は避けるが、明らかな責務の混在は避ける

## 4. React Native / Expo 方針

Expoの標準的な開発体験を優先する。

- Expo SDKに対応したパッケージを使う
- ネイティブコードの追加が必要な依存は慎重に判断する
- 位置情報、SQLite、ファイル、共有、メディアライブラリはExpo公式モジュールを優先する
- iOS / Android の権限差分を仕様と実装に明記する

## 5. ローカルファースト方針

StrolliaはローカルファーストのGPSロガーである。

- GPSログは端末内SQLiteに保存する
- 初期仕様ではサーバーを用意しない
- ユーザーの明示操作なしにGPSログや写真メタデータを外部送信しない
- バックアップはインポート・エクスポート機能で実現する

## 6. ドキュメント更新方針

仕様や設計判断を変更した場合は、関連する `docs/` のMarkdownを更新する。

特に以下に影響する変更は、ドキュメント更新を伴うこと。

- MVP範囲
- データ保存方式
- DBスキーマ
- GPS記録方式
- マップ表示方式
- インポート・エクスポート仕様
- 写真ジオタグ表示
- 権限・プライバシー方針

## 7. 作業前後の確認

作業前には既存の変更を確認し、ユーザーや他エージェントの変更を不用意に上書きしない。

作業後には以下を確認する。

- 変更したファイルの内容
- 型チェック結果(`npm run typecheck`)
- テスト結果(`npm test`)
- lint 結果(`npm run lint` — error 0 が合格条件)
- Todoの更新要否
- ドキュメントの更新要否

## 8. コメント / JSDoc 方針

コードを読む人が実装意図を追いやすいよう、コメントとJSDocを適切に付与する。

基本方針は以下とする。

- 関数、クラス、型、自明ではない変数にはJSDocを付与する
- JSDocは「何をするか」だけでなく、必要に応じて「なぜその設計・条件にしているか」を説明する
- 自明な処理に冗長なコメントを付けない
- 複雑な条件分岐、副作用、端末APIやOS制約に依存する処理には、意図が分かる短いコメントを付ける
- コメントは実装と矛盾しないよう、コード変更時に合わせて更新する

## 9. テスト記述方針

テストコードの説明文は、利用者・開発者が挙動を理解しやすいよう日本語で記載する。

ルールは以下とする。

- `describe`、`test`、`it` の説明文は日本語にする
- 期待する挙動、境界条件、失敗時の意味が分かる文にする
- 英語のAPI名や形式名が必要な場合は、日本語文中にそのまま含めてよい

## 10. このプロジェクト固有の作業ルール

過去の作業で決まった、Strollia / footspot 固有のルールを以下にまとめる。

### 10.1 worktree / ブランチ / PR

- `develop` を開発中のデフォルトブランチ、`main` をリリース状態のブランチとして扱う。
- 通常の開発作業は、先に最新の `develop` を取得し、`develop` から新しい worktree とエージェントごとの接頭辞を付けたブランチを作成してから作業する。新規 worktree の作成には `git worktree add` ではなく `git gtr new` を使い、`.env.local` をコピーするため `--no-copy` は使わない。Codex は `codex/`、Claude Code は `claude/` を使う。
- `main` はリリース反映用のブランチとして扱い、通常の開発ブランチの起点にしない。リリース作業では `develop` から `main` への PR を作成する。
- `develop` と `main` はどちらも PR 経由で変更し、直接 push しない。
- `worktreeで作業` と依頼された場合も、通常のチェックアウトではなく分離 worktree を使う。
- ただし、ユーザーが「この worktree をそのまま使って作業しても可」と明示した場合は、その worktree 内で通常の checkout をしてよい。
- `.worktrees/` はこのリポジトリで通常利用する作業場所として扱う。作成前に `git worktree list` で既存 worktree とブランチ名の衝突を確認する。
- worktree は一連の作業が完了し、PR がマージされたあとに削除する。
- 作業完了後に PR 作成を依頼された場合は、明示的に Draft と指定されない限り Open PR として作成する。
- PR の description は日本語で書く。変更内容、理由、影響範囲、検証結果を簡潔に含める。
- `コメントを取得して修正` と依頼された場合は、未解決のレビューコメントを取得し、まだ有効な指摘だけを対象に修正する。
- GitHub CLI の認証確認が不安定な場合でも、`git push` が成功していれば PR 作成が可能なことがある。CLI の一時的な失敗だけで公開作業を諦めない。

### 10.2 UI / 画面設計

- 新しい画面は `src/app/` のルートファイル(薄いラッパー) + `src/ui/components/` の画面コンポーネント(UI実体)で構成し、状態・操作は `AppStateProvider`(`src/ui/state/AppStateProvider.tsx`)から `useAppState()` 経由で取得する。
- 画面や UI を作成・変更する前に、ルートの `DESIGN.md` を参照する。デザイントークンの実値、共通コンポーネントのカタログ、画面実装の雛形が記載されている。
- 軽微な修正を除き、作業には superpowers を使う。作業前に設計書を作成してユーザーの承認を得てから、計画書を作成し、TDD で開発する。
- 新しい画面や既存画面の刷新では、カードを多用した見た目を避ける。設定画面に近い、リスト主体・帯状・必要十分なツール UI を優先する。
- 画面共通で使えるヘッダー、戻るボタン、リスト行、共有ボタンなどは、特定画面名に閉じたコンポーネント名にせず、汎用コンポーネントとして切り出して再利用する。
- 画面独自のコンポーネントは避けられるなら避ける。既存の共通コンポーネントをそのまま利用するか、必要に応じて拡張する。どうしても画面独自のコンポーネントを作る場合は、その理由を共有して承認を得る。
- 子画面への遷移は、親から子へ進むときは右から入り、戻るときは親へ左方向に戻る動きにする。地図などトップレベル画面への遷移は例外としてよい。
- iOS で子画面から親画面へ戻れる場合は、左端スワイプバックを有効にする。
- テキストは太字をデフォルトにしない。ユーザーが明示した場合、または情報階層として本当に必要な場合だけ太字を使う。
- ライトモード / ダークモードの背景色は、画面ごとに個別定義せず、アプリ共通の背景色・テーマ値を参照する。
- ボタン、ヘッダー、スライダー、スクロール可能なマップ、リスト項目など、他画面でも自然に使える部品は汎用的な名前で1ファイル1コンポーネントを基本にする。

### 10.3 GPS記録 / 地図 / インポート

- GPS記録は、権限が付与されたら自動で開始する。通常利用時の手動開始・停止ボタンは置かない。
- 自動記録を開始できなかった場合だけ、復旧用の `記録開始` ボタンを表示する。
- 地図の現在地追従は、初期状態では ON にする。ユーザーが地図をドラッグしたら OFF にし、現在地ボタンを押したときだけ再び ON にする。
- 現在地が地図中心に近づいただけで、自動的に追従 ON に戻してはいけない。
- インポート機能は、現時点では GPX のみを対象にする。設定画面などの説明文も GPX のみに合わせる。
- インポート時に既存データと競合する場合は、既存データを優先する。その挙動は UI の説明にも含める。
- ユーザーの明示操作なしに、GPSログや写真メタデータを外部送信してはいけない。

### 10.4 ドキュメント / 仕様確認

- 作業開始時に `.ai/context/` の3ドキュメントを確認する: `architecture.md`(レイヤー構成・ディレクトリ構造)、`conventions.md`(コーディング規約)、`testing.md`(テストの書き方)。
- 定型作業(画面追加、DBスキーマ変更、設定項目追加、Plus機能ゲート、ビルド、リリース、タグ付け、PRレビュー、issue作成、マージ後クリーンアップ、リリースノート作成)には `.claude/skills/` 配下の対応スキルを使う。一覧は 10.5 を参照。
- 残作業や Plus 機能の確認では、まず `docs/todo.md`、`docs/monetization.md`、`docs/plus-features.md` を確認する。
- GPX インポート作業を始めるときは、既存の設計・計画である `docs/superpowers/specs/2026-05-27-gpx-import-design.md` と `docs/superpowers/plans/2026-05-27-gpx-import.md` を先に確認する。
- 仕様やユーザー向け挙動を変えた場合は、関連する `docs/` とテストを同じ作業内で更新する。

### 10.5 定型作業スキル一覧

`.claude/skills/<スキル名>/SKILL.md` に定型作業の手順が定義されている。Claude Code・Codex を問わず、該当する作業を始める前に対応する `SKILL.md` を読み、その手順に従うこと(Claude Code はスキル呼び出し機能で自動的に読み込むが、Codex は該当ファイルを直接開いて読む)。

| スキル               | 場所                                         | 用途                                                                                |
| -------------------- | -------------------------------------------- | ----------------------------------------------------------------------------------- |
| `add-screen`         | `.claude/skills/add-screen/SKILL.md`         | 新しい画面の追加、既存画面のレイアウト・ナビゲーション再構成                        |
| `add-setting`        | `.claude/skills/add-setting/SKILL.md`        | 設定画面へのユーザー向け設定項目(トグル・選択・値)の追加、`app_settings` への永続化 |
| `build-development`  | `.claude/skills/build-development/SKILL.md`  | EASでのdevelopmentビルド(シミュレータ/実機用dev client)作成                         |
| `build-preview`      | `.claude/skills/build-preview/SKILL.md`      | EASでのpreview(内部配布)ビルド作成。リリース前検証用                                |
| `build-production`   | `.claude/skills/build-production/SKILL.md`   | EASでのproduction(ストア用)ビルド作成                                               |
| `create-issue`       | `.claude/skills/create-issue/SKILL.md`       | GitHub issue(バグ報告・機能要望・改善・バックログ)の作成                            |
| `db-schema-change`   | `.claude/skills/db-schema-change/SKILL.md`   | SQLiteのテーブル・カラム・インデックスの追加変更、リポジトリ新設                    |
| `post-merge-cleanup` | `.claude/skills/post-merge-cleanup/SKILL.md` | PRマージ後のworktree削除・ブランチ削除・develop最新化                               |
| `pr-review`          | `.claude/skills/pr-review/SKILL.md`          | PRへのインラインコメントレビュー、既存レビューコメントの取得・解消                  |
| `premium-gate`       | `.claude/skills/premium-gate/SKILL.md`       | Strollia Plus(課金)限定機能の追加・変更、RevenueCat entitlement判定、paywall導線    |
| `publish`            | `.claude/skills/publish/SKILL.md`            | 完成したproductionビルドのApp Store / Google Playへの提出                           |
| `release`            | `.claude/skills/release/SKILL.md`            | 新バージョンのリリース準備(バージョン更新、develop→mainのリリースPR)                |
| `release-notes`      | `.claude/skills/release-notes/SKILL.md`      | main(リリース済み)とdevelop(リリース予定)の差分からユーザー向けリリースノートを作成 |
| `tag-release`        | `.claude/skills/tag-release/SKILL.md`        | リリースPRマージ後、mainの最新コミットへバージョンタグを付与                        |
| `worktree-setup`     | `.agents/skills/worktree-setup/SKILL.md`     | Footspot の新規 worktree 作成。`git gtr new` による `.env.local` のコピーを含む     |
